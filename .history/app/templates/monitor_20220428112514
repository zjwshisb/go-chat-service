{{define "monitor.tmpl"}}
<html>
  <style>
    .body{
      display: flex;
      align-items: center;
      flex-direction: column;
    }
  </style>
  <body class="body">
    <div>
      {{.admin}}
      {{.user}}
    </div>
  </body>
</html>
{{ end }}